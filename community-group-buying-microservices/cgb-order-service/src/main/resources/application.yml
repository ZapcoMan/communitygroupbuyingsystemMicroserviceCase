server:
  port: 8004

spring:
  application:
    name: cgb-order-service
  profiles:
    active: dev

  # ==================== Nacos 配置中心 ====================
  cloud:
    nacos:
      server-addr: ${NACOS_ADDR:127.0.0.1:8848}
      discovery:
        namespace: cgb-dev
        group: CGB_GROUP
        metadata:
          version: 1.0.0
          env: dev
      config:
        namespace: cgb-dev
        group: CGB_GROUP
        file-extension: yml
        shared-configs:
          - data-id: common-redis.yml
            group: SHARED_GROUP
            refresh: true
          - data-id: common-mybatis.yml
            group: SHARED_GROUP
            refresh: true
        refresh-enabled: true
  # 从 Nacos 导入配置（数据源、Redis、RocketMQ、Seata 等敏感配置均在 Nacos 管理）
  config:
    import:
      - optional:nacos:cgb-order-service.yml?group=CGB_GROUP&refreshEnabled=true

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cgb.order.entity
  configuration:
    map-underscore-to-camel-case: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: when_authorized
  metrics:
    tags:
      application: ${spring.application.name}
  tracing:
    sampling:
      probability: 0.1
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_URL:http://localhost:9411/api/v2/spans}

logging:
  level:
    com.cgb: debug
    com.alibaba.nacos: info
